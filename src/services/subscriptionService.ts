// Billing is intentionally dormant in the personal release. Secret operations
// must be implemented in Supabase Edge Functions when monetization is enabled.
export const subscriptionService = { isPremiumEnabled: false } as const;
