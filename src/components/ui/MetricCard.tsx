import { Box, Flex, Heading, Text } from '@chakra-ui/react';import type { ReactNode } from 'react';
export function MetricCard({label,value,help,icon,tone='brand.300'}:{label:string;value:string;help?:string;icon?:ReactNode;tone?:string}){return <Box bg="panel" border="1px solid" borderColor="line" borderRadius="2xl" p="5" boxShadow="card" transition=".22s ease" _hover={{transform:'translateY(-2px)',borderColor:'brand.500'}}><Flex justify="space-between" color={tone}><Text color="muted" fontSize="sm">{label}</Text>{icon}</Flex><Heading size="md" mt="3" color={value.startsWith('-')?'red.300':undefined}>{value}</Heading>{help&&<Text color="muted" fontSize="xs" mt="1">{help}</Text>}</Box>}
