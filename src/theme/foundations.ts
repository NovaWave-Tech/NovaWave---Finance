import type { ThemeConfig } from '@chakra-ui/react';
export const config: ThemeConfig = { initialColorMode: 'dark', useSystemColorMode: false };
export const fonts = { heading: "'Manrope',sans-serif", body: "'DM Sans',sans-serif" };
export const radii = { xl: '16px', '2xl': '22px' };
export const shadows = { card: '0 18px 48px rgba(0,0,0,.22)' };
